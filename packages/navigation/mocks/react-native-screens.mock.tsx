import * as React from "react";

export function ScreenStack(props: { children?: React.ReactNode }) {
  return <>{props.children}</>;
}

export function Screen(props: { children?: React.ReactNode }) {
  return <>{props.children}</>;
}
